# Пусто намеренно.
#
# В образе nginx на этом месте лежит его собственный пример: сервер на 80 порту, отдающий
# страницу «welcome to nginx». Он не мешал бы работать, но оказался бы ПЕРВЫМ сервером на порту,
# то есть тем, кому достаются запросы без совпадения — и однажды посетитель увидел бы вместо
# продукта заглушку. Файл перекрыт пустотой, чтобы примера не было вовсе.
